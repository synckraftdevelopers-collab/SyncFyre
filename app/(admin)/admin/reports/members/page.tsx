import Link from "next/link";
import { getMembersReport } from "@/services/report.service";
import { requireUser } from "@/lib/auth";
import { Card } from "@/components/ui/card";
import { buttonVariants } from "@/components/ui/button";
export const metadata = { title: "Members Report" };
export default async function MembersReport({ searchParams }: { searchParams: Promise<{ q?: string; status?: "active" | "inactive" | "all"; page?: string }> }) { const sp = await searchParams; const profile = await requireUser(["admin", "manager"]); const report = await getMembersReport({ branchId: profile.branch_id, page: Math.max(1, Number(sp.page ?? 1)), pageSize: 50, status: sp.status, search: sp.q }); return <div className="space-y-5"><div className="flex items-center gap-3"><div><h1 className="text-2xl font-bold">Members Report</h1><p className="text-sm text-muted-foreground">{report.total} member records</p></div><Link href="/api/reports?resource=members" className={buttonVariants({ variant: "outline", className: "ml-auto" })}>Export CSV</Link></div><Card><form className="flex gap-3 border-b p-4"><input name="q" defaultValue={sp.q} placeholder="Search member" className="h-10 flex-1 rounded-lg border bg-background px-3 text-sm" /><select name="status" defaultValue={sp.status ?? "all"} className="h-10 rounded-lg border bg-background px-3 text-sm"><option value="all">All statuses</option><option value="active">Active</option><option value="inactive">Inactive</option></select><button className={buttonVariants({ variant: "outline" })}>Apply</button></form><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-sm"><thead className="border-b text-left text-xs uppercase text-muted-foreground"><tr>{["Code", "Name", "Phone", "Gender", "Plan", "Expiry", "Status", "Joined"].map((h) => <th className="p-3" key={h}>{h}</th>)}</tr></thead><tbody className="divide-y">{report.data.map((row: any) => <tr key={row.member_id}><td className="p-3 font-mono text-xs">{row.member_code}</td><td className="p-3 font-medium">{row.full_name}</td><td className="p-3">{row.phone}</td><td className="p-3">{row.gender ?? "—"}</td><td className="p-3">{row.current_plan ?? "—"}</td><td className="p-3">{row.expiry_date ?? "—"}</td><td className="p-3 capitalize">{row.member_status}</td><td className="p-3">{row.joined_date}</td></tr>)}</tbody></table></div></Card></div>; }
