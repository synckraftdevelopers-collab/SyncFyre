import { getMonthlyRevenueSummary } from "@/services/report.service";
import { requireUser } from "@/lib/auth";
import { Card } from "@/components/ui/card";
export const metadata = { title: "Revenue Report" };
export default async function RevenueReport() { const profile = await requireUser(["admin", "manager"]); const rows = await getMonthlyRevenueSummary({ branchId: profile.branch_id }); return <div className="space-y-5"><div><h1 className="text-2xl font-bold">Revenue Report</h1><p className="text-sm text-muted-foreground">Monthly revenue including refunds and net collections.</p></div><Card><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead className="border-b text-left text-xs uppercase text-muted-foreground"><tr>{["Month", "Transactions", "Gross", "Refunds", "Net Revenue"].map((h) => <th className="p-3" key={h}>{h}</th>)}</tr></thead><tbody className="divide-y">{rows.map((row) => <tr key={`${row.revenue_month}-${row.branch_name}`}><td className="p-3 font-medium">{row.revenue_month_label}</td><td className="p-3">{row.transaction_count}</td><td className="p-3">₹{row.gross_amount.toLocaleString("en-IN")}</td><td className="p-3">₹{row.total_refunds.toLocaleString("en-IN")}</td><td className="p-3 font-semibold">₹{row.net_revenue.toLocaleString("en-IN")}</td></tr>)}{!rows.length && <tr><td className="p-8 text-center text-muted-foreground" colSpan={5}>No revenue data yet.</td></tr>}</tbody></table></div></Card></div>; }
