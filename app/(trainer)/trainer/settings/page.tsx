import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { requireUser } from "@/lib/auth"; import { updateTrainerProfileAction } from "@/app/actions/notification-actions";
export default async function TrainerSettingsPage(){const profile=await requireUser(["trainer", "dietician", "diet-planner", "diet_planner"]);return <div className="mx-auto max-w-2xl space-y-5"><div><h1 className="text-2xl font-bold">Personal settings</h1><p className="text-sm text-muted-foreground">Update your personal contact details.</p></div><Card><CardHeader><CardTitle>Profile</CardTitle></CardHeader><CardContent><form action={updateTrainerProfileAction} className="space-y-4"><label className="block text-sm font-medium">Full name<input name="full_name" required defaultValue={profile.full_name} className="mt-1.5 h-10 w-full rounded border px-3"/></label><label className="block text-sm font-medium">Phone<input name="phone" defaultValue={profile.phone??""} className="mt-1.5 h-10 w-full rounded border px-3"/></label><p className="text-xs text-muted-foreground">Role, branch, and permissions are managed by your administrator.</p><button className="rounded bg-primary px-4 py-2 text-primary-foreground">Save changes</button></form></CardContent></Card></div>}